// Loops - Iterando arrays


// Usando 'for'
let languages = ["JavaScript", "Python", "Php", "Java", "C#"];
for (i = 0; i < languages.length; i++) {
    console.log(languages[i]);
}

// Usando 'for..of'
let users = ['João', 'Maria', 'Pedro', 'Ana']
for (let user of users) {
    console.log(user);
}

console.log(users.length);