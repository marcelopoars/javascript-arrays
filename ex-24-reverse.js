// O métoro 'arr.reverse()' inverte a ordem dos elementos de um array

// Exemplo:
let numbers = [1, 2, 3, 4, 5];

let invertedNumbers = numbers.reverse()

console.log({numbers});
console.log({invertedNumbers});
